import React, { useState } from 'react';
import './App.scss';

function App() {
  const [auth, setAuth] = useState(false);

  return (
    <div className="App">
      {/* here we go */}
    </div>
  );
}

export default App;